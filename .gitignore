.env
__pycache__/
*.pyc
.claude/
.cache/

# 52.5MB, 지금 어떤 코드도 안 읽음(Poppler 없음 + 20MB 읽기 한도로 이 세션에서도 못 읽었음,
# app/policy_rag.py RAG 대상에도 없음) — 커밋해봐야 리포 용량만 늘고 쓸모가 없다.
# 나중에 실제로 이걸 처리할 방법이 생기면 그때 커밋한다. (gitignore는 따옴표로 공백을
# 묶으면 안 됨 — 따옴표 자체가 패턴 문자로 취급돼 매칭이 안 된다.)
data/방역공식자료/역학조사관 교육 표준교재 1권.pdf

# 51MB, 전국 가축사육업 인허가 원본(지방행정인허가데이터개방시스템) — 실제로 쓰는 건
# 이 파일에서 뽑아낸 산출물인 data/farms_geocoded.csv뿐이다(scripts/ingest_national_licenses_2026.py
# 참고). 재현하려면 이 원본 파일을 다시 받아와야 하지만, 커밋해서 리포에 51MB를 얹을
# 만큼의 가치는 없다고 판단(같은 디렉터리의 나머지 작은 파일들은 유지 — 향후 사육두수
# 보완 등에 참고 가능).
data/농가현황자료/동물_가축사육업.csv

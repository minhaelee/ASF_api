.env
__pycache__/
*.pyc
.claude/
.cache/

# 52.5MB, 지금 어떤 코드도 안 읽음(Poppler 없음 + 20MB 읽기 한도로 이 세션에서도 못 읽었음,
# app/policy_rag.py RAG 대상에도 없음) — 커밋해봐야 리포 용량만 늘고 쓸모가 없다.
# 나중에 실제로 이걸 처리할 방법이 생기면 그때 커밋한다. (gitignore는 따옴표로 공백을
# 묶으면 안 됨 — 따옴표 자체가 패턴 문자로 취급돼 매칭이 안 된다.)
data/방역공식자료/역학조사관 교육 표준교재 1권.pdf
